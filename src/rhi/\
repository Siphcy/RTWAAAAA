#include "rhi.hpp"
#include <vector>
namespace RTWA {
class VertexArray {
public:
  GLuint VAO, VBO; // Vertex Array Object, Vertex Buffer Object
  VertexArray();
  void addVertices(GLfloat &vertices[]);

private:
  std::vector<GLfloat[]> VertexVector;
};
} // namespace RTWA
